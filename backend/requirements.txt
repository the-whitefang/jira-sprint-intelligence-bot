# ---------------------------------------------------------------------------
# Jira Sprint Intelligence Bot — Backend Runtime Dependencies
# Python 3.12
# ---------------------------------------------------------------------------

# --- Web framework ---------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==23.0.0
python-multipart==0.0.12

# --- Settings & validation ---------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Database ----------------------------------------------------------------
sqlalchemy==2.0.35
alembic==1.13.3
asyncmy==0.2.9              # async MySQL driver used by SQLAlchemy's async engine

# --- Cache / queue -------------------------------------------------------
redis==5.1.1

# --- Vector store --------------------------------------------------------
chromadb==0.5.15

# --- LLM integration -------------------------------------------------------
google-generativeai==0.8.3
tenacity==9.0.0             # retry/backoff for Gemini + external calls

# --- Auth / security -------------------------------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- HTTP client (Jira API calls, internal service calls) ------------------
httpx==0.27.2

# --- Logging -----------------------------------------------------------------
python-json-logger==2.0.7

# --- Config loading ------------------------------------------------------
python-dotenv==1.0.1
