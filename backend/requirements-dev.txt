# ---------------------------------------------------------------------------
# Dev / test dependencies — installed on top of requirements.txt
# Not installed in the production image.
# ---------------------------------------------------------------------------
-r requirements.txt

pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
aiosqlite==0.20.0           # lightweight async DB for unit tests
faker==30.3.0
mypy==1.11.2
ruff==0.6.9
black==24.10.0
pre-commit==4.0.1
